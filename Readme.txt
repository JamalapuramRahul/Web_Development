SQL - 

Copy the sql file and open it in the mysql workbench
create the database and use the databas
Create tables and insert appropriate values into the tables using insert statements.

Spring boot

Open the maven project in eclipse and run the application
after tomcat is started run the frontend application.

react
open the frontend folder in vs code and use 
use npm install to install all the node modules required for the project
npm start and the application will start login and use the application